# 🏷️ 标的管理 SOP

> 暗号：`关注 XXX` / `建仓 XXX` / `移除关注 XXX` / `清仓 XXX`
> 模块：持仓与关注/（状态层）

---

## 一句话定位

标的管理做两件事：**维护「持仓与关注」的状态真相源** + **触发初始调研（同步或异步）**

- 关注 = 创建目录 + 初始化文档 + 智能触发初始调研
- 建仓/清仓只是状态迁移，不包含买卖决策（那是策略分析的事）
- 初始调研的实际执行由 `Brain/SOP_初始调研.md` 负责

---

## 🔑 触发暗号

| 暗号 | 动作 | 说明 |
|:---|:---|:---|
| `关注 [ticker/名称]` | 新增到 Watchlist + 触发初始调研 | 询问是否立即调研，否则异步 |
| `建仓 [ticker/名称]` | 从 Watchlist 升级到持仓 | 状态迁移 + 前置检查 |
| `移除关注 [ticker/名称]` | 从 Watchlist 归档 | 状态迁移 + 归档 |
| `清仓 [ticker/名称]` | 从持仓归档（保留观察） | 状态迁移 + 复盘提醒 |

---

## ⚙️ 执行步骤

### 动作 A：关注（新增 Watchlist + 触发初始调研）

**Step 1：确认标的信息 + 自动补全 Ticker 后缀**

- 解析 ticker（如 `NVDA`、`腾讯`、`BTC`、`688048`）
- 确认市场（US/A股/H股/Crypto）
- 确定目录名格式：`名称_TICKER`

**A股/港股 Ticker 后缀自动识别规则**：

| 代码特征 | 交易所 | Yahoo 后缀 | 示例 |
|:---|:---|:---|:---|
| 6位数字，`6` 开头 | 上交所 | `.SS` | `688981` → `688981.SS` |
| 6位数字，`0` 或 `3` 开头 | 深交所 | `.SZ` | `300274` → `300274.SZ` |
| 4-5位数字（港股） | 港交所 | `.HK` | `700` → `0700.HK` |
| 纯英文字母（美股） | NYSE/NASDAQ | 无后缀 | `NVDA` |
| Crypto | 去中心化 | 无后缀 | `BTC` |

**Step 2：创建文档目录**

```bash
mkdir -p "持仓与关注/动态Watchlist/名称_TICKER"
```

**Step 3：初始化文档**

从模板创建：
- `持仓与关注/_模板_单标的/个股基础知识库_模板.md` → `个股基础知识库.md`
- Watchlist 不需要买入卖出备忘录

> 若模板不存在，创建最小骨架文件

**Step 4：写入状态机**

在 `持仓与关注/Watchlist_状态机.md` 新增一行：
- Ticker、名称、市场、加入日期、状态（关注中）
- P4 摘要和 P2 阶段留空（待初始调研完成后填充）

**Step 5：触发初始调研（智能切换）**

| 场景 | 行为 |
|:---|:---|
| 单个关注（`关注 NVDA`） | 询问用户：「是否现在跑初始调研？」→ 是则同步调用 `Brain/SOP_初始调研.md`，否则异步 |
| 批量关注（一次多个标的） | 第一个询问，其余异步（投资雷达下次会建议调研） |

> 异步 = 投资雷达报告的「调研建议」区块会捕获未建档标的，自然形成调研触发。

**Step 6：在对话中确认**

```
✅ 已关注 [名称]（[TICKER]）
- 目录：持仓与关注/动态Watchlist/名称_TICKER/
- 个股基础知识库已初始化
- Watchlist 状态机已更新
- [初始调研已完成 / 初始调研将在下次投资雷达中触发]

💡 下一步建议：
- 如需立即调研 → 说「调研一下 [TICKER]」
```

---

### 动作 B：建仓（Watchlist → 持仓）

**前置**：标的必须已在 Watchlist 中，且 P1+P4+P2 已完成

**Step 1**：检查前置
- 确认 `动态Watchlist/名称_TICKER/个股基础知识库.md` 存在且有 P1+P4+P2
- **若标的不在 Watchlist**：自动先执行「动作 A：关注」完整流程
- **若缺少 P1/P4/P2**：调用 `Brain/SOP_初始调研.md` 补跑

**Step 2**：迁移目录
```bash
mv "持仓与关注/动态Watchlist/名称_TICKER" "持仓与关注/持仓/名称_TICKER"
```

**Step 3**：初始化买入卖出备忘录
- 从模板创建 `买入卖出备忘录.md`（持仓才需要）

**Step 4**：更新状态机
- `Watchlist_状态机.md`：移除或标记「已建仓」
- `持仓_状态机.md`：新增，状态「持仓中」

**Step 5**：确认
```
✅ [名称]（[TICKER]）已从 Watchlist 升级为持仓
- 目录：持仓与关注/持仓/名称_TICKER/
- 买入卖出备忘录已初始化
- 下一步：建议执行「我想买 TICKER」进入策略分析
```

---

### 动作 C：移除关注（Watchlist → 归档）

**Step 1**：归档目录
```bash
mkdir -p "持仓与关注/动态Watchlist/_archive"
mv "持仓与关注/动态Watchlist/名称_TICKER" "持仓与关注/动态Watchlist/_archive/名称_TICKER"
```

**Step 2**：更新 `Watchlist_状态机.md`（标记「已归档」+ 日期）

---

### 动作 D：清仓（持仓 → Watchlist 保留观察）

**Step 1**：迁移到 Watchlist
```bash
mv "持仓与关注/持仓/名称_TICKER" "持仓与关注/动态Watchlist/名称_TICKER"
```

**Step 2**：更新状态机
- `持仓_状态机.md`：移除或标记「已清仓」
- `Watchlist_状态机.md`：新增，状态「观察中（已清仓）」

**Step 3**：在 `买入卖出备忘录.md` 追加清仓记录

**Step 4**：生成复盘提醒
- 在 `持仓与关注/投资复盘/` 下创建 `YYYY-MM-DD_TICKER_清仓复盘.md`

---

## 📦 产出物清单

| 文件 | 路径 | 说明 |
|:---|:---|:---|
| 个股基础知识库 | `持仓与关注/{位置}/名称_TICKER/个股基础知识库.md` | 关注时初始化 |
| 买入卖出备忘录 | `持仓与关注/持仓/名称_TICKER/买入卖出备忘录.md` | 建仓时初始化 |
| 状态机 | `持仓与关注/Watchlist_状态机.md` / `持仓_状态机.md` | 每次更新 |

---

## 📁 路径速查

| 内容 | 路径 |
|:---|:---|
| 初始调研 SOP | `Brain/SOP_初始调研.md` |
| P 提示词目录 | `skills/<skill>/prompts/` 或 `skills/ymos-core/prompts/` |
| 模板 | `持仓与关注/_模板_单标的/` |
| 状态机 | `持仓与关注/Watchlist_状态机.md` / `持仓_状态机.md` |

---

## ⚠️ 边界

- 标的管理的**分析部分由初始调研子模块 SOP 负责**
- 建仓/清仓只是状态迁移，**不包含买卖决策**
- 目录命名统一为 `名称_TICKER`，价格扫描只认状态机 Ticker 列
- Watchlist 不需要买入卖出备忘录（建仓时才初始化）

---

*SOP 版本：2026-03-18 · YMOS V3 三模块制（Eyes / Brain / 持仓与关注）*
